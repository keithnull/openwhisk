include classpath("standalone.conf")

whisk {
  config {
    # increase the rate limit for stress testing system performance
    limits-actions-invokes-perMinute = 100
    limits-actions-invokes-concurrent = 100
  }
}
